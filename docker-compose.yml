services:
  vibrails-server:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env.example
    command: >
      sh -c "pip install --no-cache-dir -e . &&
      uvicorn server.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
